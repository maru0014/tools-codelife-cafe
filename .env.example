# Cloudflare Web Analytics のサイトトークン。
# Cloudflare ダッシュボード > Web Analytics でサイトを追加すると発行される
# JS スニペット内の data-cf-beacon の token 値を設定する。
# 未設定の場合、ビーコンは出力されない（開発・プレビュー時は空でよい）。
# 本番は GitHub Actions の Variables（vars.PUBLIC_CF_BEACON_TOKEN）から注入される。
PUBLIC_CF_BEACON_TOKEN=

---
import ThemeToggle from '../common/ThemeToggle.tsx';
---

<header class="sticky top-0 z-50 border-b border-border bg-background/80 backdrop-blur-lg">
  <div class="mx-auto flex h-16 max-w-6xl items-center justify-between px-4 sm:px-6">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-2 font-bold text-lg tracking-tight hover:opacity-80 transition-opacity">
      <span class="text-primary">🔒</span>
      <span class="hidden sm:inline">CODE:LIFE Tools</span>
      <span class="sm:hidden">C:L Tools</span>
    </a>

    <!-- Center: Search hint -->
    <div class="hidden md:flex items-center">
      <button
        type="button"
        class="flex items-center gap-2 rounded-lg border border-border bg-muted/50 px-4 py-1.5 text-sm text-muted-foreground hover:bg-muted transition-colors"
        id="search-trigger"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path></svg>
        <span>ツールを検索...</span>
        <kbd class="ml-2 rounded border border-border bg-background px-1.5 py-0.5 text-xs font-mono">⌘K</kbd>
      </button>
    </div>

    <!-- Right: Theme toggle -->
    <div class="flex items-center gap-3">
      <span class="hidden lg:flex items-center gap-1.5 text-xs text-safety font-medium">
        <span class="inline-block h-2 w-2 rounded-full bg-safety pulse-dot"></span>
        オフライン対応
      </span>
      <ThemeToggle client:load />
    </div>
  </div>
</header>
